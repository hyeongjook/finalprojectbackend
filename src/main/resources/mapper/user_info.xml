<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace는 반드시 mapper 위치로 설정해야 된다. -->
<mapper namespace="com.ict.finalspringboot.domain.user_info.mapper.UserMapper">

   <!-- 특정 사용자 조회 -->
<select id="getusersById" parameterType="int" resultType="com.ict.finalspringboot.domain.user_info.vo.userVO">
    SELECT user_idx, user_id, user_pw, user_profile, user_name, user_nickname, user_email, user_reg_date,
     user_phone, user_birth_date, user_gender, user_level_idx
    FROM user_db.user_info_tbl
    WHERE user_idx = #{user_idx}
</select>

<!-- 사용자 목록 조회 -->
<select id="userinfoList" resultType="com.ict.finalspringboot.domain.user_info.vo.userVO">
    SELECT user_idx, user_id, user_pw, user_profile, user_name, user_nickname, user_email, user_reg_date,
           user_phone, user_birth_date, user_gender, user_level_idx
    FROM user_db.user_info_tbl
</select>


    <!-- 사용자 추가 -->
    <insert id="insertUser" parameterType="userVO" useGeneratedKeys="true" keyProperty="userIdx">
    INSERT INTO user_db.user_info_tbl (user_id, user_pw, user_profile, user_name, user_nickname,  user_reg_date,
    user_email, user_phone, user_birth_date, user_gender)
    VALUES (#{user_id}, #{user_pw}, #{user_profile}, #{user_name}, #{user_nickname}, #{user_email},
     #{user_phone}, #{user_birth_date}, #{user_gender})
    </insert>

    <!-- 사용자 수정 -->
    <update id="updateUser" parameterType="userVO">
        UPDATE user_db.user_info_tbl
        SET user_pw = #{user_pw}, user_profile = #{user_profile}, user_name = #{user_name},  user_reg_date = #{user_reg_date}
        user_nickname = #{user_nickname}, user_email = #{user_email}, user_phone = #{user_phone},
         user_birth_date = #{user_birth_date}, user_gender = #{user_gender}
        WHERE user_idx = #{user_idx}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="deleteUser" parameterType="int">
        DELETE FROM user_db.user_info_tbl WHERE user_idx = #{user_idx}
    </delete>

</mapper>
